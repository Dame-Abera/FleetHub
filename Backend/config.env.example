# Database Configuration (Prisma format)
DATABASE_URL="postgresql://username:password@localhost:5432/car_marketplace?schema=public"

# Alternative separate database config (if needed)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=car_marketplace

# Application Configuration
NODE_ENV=development
PORT=3001
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# AI Configuration (if using AI features)
OPENAI_API_KEY=your-openai-api-key-here 